# 成就搜索显式提交设计

成就总览在输入关键词时保持当前结果、数量和分区不变。搜索框右侧增加“搜索”按钮；点击按钮后才将输入内容写入 `state.achievementSearch` 并重新渲染。回车继续调用同一提交逻辑，输入法确认中的回车不触发搜索。

实现只修改成就专用模块与样式。浏览器回归检查验证：输入阶段仍显示 141 项，搜索按钮存在，点击后 `Thanks, Obama` 收窄为对应的一项成就，并且提交后光标仍在输入内容末尾。
