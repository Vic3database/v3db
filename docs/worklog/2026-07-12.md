# 工作记录：2026-07-12

## 2026-07-12 关于浮窗、公开 README 与国内域名方案

继续修正地区列表中的农业潜力图标。地区结果卡片里的农业潜力原本复用 `.building-chip`，因此每个农业建筑 PNG 外面仍有边框、深色底板和内层图标底板。本轮只对 `.results .region-row .arable-chip` 做覆盖，去掉外层边框、背景和阴影，并去掉内层 `.resource-icon` 的背景、阴影和圆角；有数量的矿产和其他资源项仍保留原来的数量芯片结构。`scripts/check_right_panel_layout.mjs` 已补充对应合同，`site/index.html` 的样式缓存参数更新为 `20260710-arable-icons1`。

继续修正左栏资源筛选按钮。截图中的问题来自两层样式叠加：资源筛选按钮外层仍被早期的图片按钮选中态画出半透明金色底板，内层建筑 PNG 又继承了通用 `.resource-icon` 的深色背景、圆角和内阴影，于是看起来像 PNG 外面套了小卡片。本轮将图片型筛选按钮的 hover 和选中背景都改为透明，选中态保留金色边框和一圈内描边；资源筛选里的 `.resource-icon` 去掉背景、阴影和圆角，只显示 PNG 本身。`scripts/check_left_sidebar_buttons.mjs` 已补充对应合同，`site/index.html` 的样式缓存参数更新为 `20260710-resource-gold-border1`。

继续修正列表卡片内部行高。卡片整体已经允许自然增高后，标题、编号和部分标签子行仍有 `nowrap`、省略号和隐藏溢出规则，导致 `石勒苏益格-荷尔斯泰因（斯莱斯维格-奥尔萨斯）` 这类长名称仍会被截断。本轮将结果列表中的国家、文化、公司、地区和意识形态标题行、名称行与编号行统一改为 `max-height: none`、`overflow: visible`、`white-space: normal` 和 `overflow-wrap: anywhere`，固定尺寸只保留给图标、徽标等视觉元素。地区板块也补上了选中州地区后的地图聚焦：点击地区卡片仍停留在 `#/region` 并高亮列表，同时地图按所选州地区定位到可识别的大致区域；地图异步加载完成后会恢复国家、地区或公司当前选中对象的焦点。`site/index.html` 的脚本和样式缓存参数更新为 `20260710-card-lines-region-focus1`，`scripts/check_right_panel_layout.mjs` 增加了子行换行与地区焦点合同。

继续调整国家和公司选中后的地图聚焦。用户反馈原来的聚焦拉得过近，难以识别世界地图方位，因此 `focusStateRegionsOnMap` 改为按整张世界地图适配比例计算最大缩放，国家和公司分别使用 `maxWorldScale` 限制，配合更大的上下文留白，只把视角带到大致区域。公司地图标签也改为只在同一州地区关联多家公司时显示数量，单个公司的关联点不再显示 `1`。`site/index.html` 的脚本缓存参数更新为 `20260710-map-focus1`，`scripts/check_country_map_selection.mjs` 增加了聚焦缩放和公司标签合同。

修正国家板块选中重大统一或可成立国家时没有显示默认领土的问题。根因是国家地图层一直使用开局省份拥有者像素图着色，安第斯联邦 `FND` 开局不存在，虽然数据里有 35 个 `formationStateRegions` 和 `geographic_region_andean_federation`，但选中后没有把这些成立范围州地区传给地图。现已在 `site/app.js` 中加入 `countryMapStateKeys`、`countryMapStateRegions`、`countryMapUsesOwnerPixels` 和 `focusCountryOnMap`：选中国家时优先使用成立范围州地区，其次使用规则直接列州、成立地理区域、开局州和释放州；有选中范围时地图停用开局拥有者像素着色，改用国家颜色高亮默认领土，并把非范围陆地区域淡化。`site/index.html` 的脚本缓存参数更新为 `20260710-country-territory1`。新增 `scripts/check_country_map_selection.mjs`，覆盖 `FND` 数据存在、重大统一标记、成立州地区数量、安第斯战略区域引用，以及国家地图选中后的范围、缓存签名和像素着色开关合同。
