# 工作记录：2026-07-10

## 2026-07-10 右栏列表、地图悬停与筛选规则继续调整
后续待办包括：调整公司列表卡片高度；调整左栏建筑选中时的表现；对所有详情页面加入返回键，并修改返回键图标；调整各板块左栏分区的默认折叠情况；调整选中某一公司时的地区缩放比例；调整各种标签颜色，提高深色界面下的对比度；调整悬停内容的信息分级；调整设置界面效果；将更新日志移成一个单独板块；整理素材库，并重新批量转换 PNG。素材整理时需要同时考虑公司图标、DLC 图标、建筑图标、名贵商品图标和未来板块可能复用的图标命名规则。

本轮更新了 `site\app.js`、`site\styles.css` 和 `scripts\check_ui_ideology_contracts.mjs`。已执行 `node --check site\app.js`、`node --check site\versions.js`、`node --check scripts\check_ui_ideology_contracts.mjs`、`node scripts\check_ui_ideology_contracts.mjs`、`node scripts\check_filter_order.mjs --file site\index.html` 和 `git diff --check`；其中 `git diff --check` 只提示这几个已修改文件以后会按 CRLF 处理，没有发现空白错误。浏览器验证使用 `http://127.0.0.1:8891/index.html?visual=company-redesign-20260709#/company`，页面标题为“公司 - Vicdata”，公司数量为 221，列表实际渲染 221 条；DLC 图标为 28 像素，名贵商品图标正常加载，详情页图片没有加载失败，控制台没有错误。

公司详情入口已经拆分。点击公司卡片正文会选中公司，仍停留在 `#/company`，并让地图聚焦该公司相关地区；点击公司图标或中文名会进入 `#/company/<公司ID>` 详情页。公司详情页已加入返回按钮，返回后仍保留当前选中的公司。建筑图标的概念浮窗也已修正，非链接图标只提示右键搜索，不再显示“左键打开”。

名贵商品图标已从 `D:\YukimiBot\V3&人文社科\公司图鉴\特殊名贵商品` 和 `D:\YukimiBot\V3&人文社科\公司图鉴\通用名贵商品` 复制到 `site\assets\prestige-goods`。当前目录共有 72 个 PNG 文件，站点数据里 71 个名贵商品键均能找到对应图标。公司列表和公司详情页现在都使用名贵商品图标加中文名称显示。总部倾向的多个地区使用 `/` 分隔，表示候选关系；限定文化也沿用同样的简洁文本。彩蛋公司改为最后一行标签，不再写入公司元数据文本。

公司列表进行了第一轮结构重排。右侧列表栏宽度从 `clamp(280px, 20vw, 380px)` 调整为 `clamp(420px, 30vw, 570px)`；公司卡片改成公司图标、中文名、英文 ID 和 DLC 图标占用头部两行，图标高度与两行标题区域一致。主营建筑和拓展建筑改为图标行显示，拓展建筑不再使用虚线框，主营与拓展之间用圆点分隔。基础版 `Victoria 3` 不再在公司列表里强调；需要 DLC 的公司保留 DLC 图标，图标尺寸已经调大。史实公司不再在公司 ID 下重复标注。

本轮围绕人民之声 DLC 的深灰、深蓝和金色方向继续调整站点视觉。站点已经改为单一主题，不再保留“深色主题”作为可切换项；`site\styles.css` 中的基础色改为深灰背景、深蓝表头、蓝色按钮和金色选中边框，并加入较轻的纹理层。字体试验从“正文和标题使用思源宋体，按钮、标签和数字保留无衬线”推进到“整体使用思源宋体”，同时固定了基础字号、标签字号和控件高度。地图浮窗和概念浮窗按照新字号放大，PNG 图标类标签去掉药丸边框，以减少无意义占位面积。

## 2026-07-10 国家地图选中成立领土修正
已新增并更新 `scripts/check_right_panel_layout.mjs`，覆盖右栏排版、标签对比度、地图悬停字段、列表点击契约、地区筛选和建筑筛选选中态。已执行 `node --check site/app.js`、`node --check scripts/check_right_panel_layout.mjs` 和 `node scripts/check_right_panel_layout.mjs`。浏览器验证使用 `http://127.0.0.1:8010/#/region`：地理区域筛选后地址仍为 `#/region`，列表显示 145 个州地区且没有战略区域或地理区域行；点州地区卡片只高亮，点名称进入 `#/state-region/STATE_PROVENCE`，详情页保留 145 条列表并隐藏地图和筛选栏。国家、文化、地区、公司与海域的地图悬停字段已用真实鼠标悬停抽样确认。地区卡片和意识形态卡片没有横向溢出，整张卡片没有纵向裁切；地区卡片内部标签行和资源行最多显示两行。

地图悬停已按板块收窄字段。国家悬停显示开局归属、当前省份归属、战略区域和本土文化；文化悬停显示开局归属、当前省份归属、本土文化和文化关系；地区悬停显示战略区域、资源、耕地和地区特质；公司悬停显示开局归属、战略区域、资源和地区特质；海域只显示名称和 ID。详情页布局也已调整为左侧保留当前列表、右侧显示详情，地图和筛选栏在详情页隐藏，并加入左右面板进入动画。

本轮已完成上述交互调整。国家、文化、地区和意识形态列表现在都采用“点卡片选中，点名称进入详情”的逻辑；地区列表只渲染州地区，旧的战略区域、地理区域列表模式按钮和对应右栏行已经移除。地理区域筛选改为与战略区域筛选一致的分组行样式，选中筛选项只更新 `#/region` 下的筛选结果，不会跳到 `#/geographic-region/...`；建筑筛选选中态改为当前深色配色中的金色边框高亮，不再使用旧角标。

同一轮还需要修正地区和意识形态列表卡片的换行问题，并统一列表交互：点击卡片只选中和高亮，点击名称或专门按钮才进入详情页。地区列表只显示州地区，不再混入战略区域或地理区域；筛选区选择战略区域或地理区域只作为筛选条件，不再进入对应详情页。地理区域筛选需要改成和战略区域筛选一致的分组格式。建筑筛选选中态需要使用当前配色方案下的新高亮方式。

本轮接续 `codex/vicdata-frontend-redesign` 分支处理右栏列表和地图悬停问题。用户截图显示地图浮层在不同地图模式下仍然显示过多字段，尤其是地区名称、战略区域、开局归属、本土文化、公司关联、资源、耕地和地区特质同时出现，导致浮层变宽变高。新的规则是：所有板块都显示地区名称、ID、名称变体；国家板块只补充开局归属、当前省份归属、战略区域、本土文化；文化板块只补充开局归属、当前省份归属、本土文化、与选中文化之间的关系；地区板块只补充战略区域、资源潜力、耕地数量、地区特质；公司板块只补充开局归属、战略区域、资源潜力、地区特质；海域只显示名字和 ID。

继续修正列表卡片高度问题。上一轮虽然取消了卡片自身的固定高度，但 `.results .pill-line` 和 `.results .country-meta` 仍然保留一行高度和隐藏溢出，导致标签较多的卡片仍然显示不全。本轮将结果列表内的标签行、元信息行、地区资源行、地区标签行和意识形态法律预览都改为 `max-height: none` 与 `overflow: visible`，并移除了意识形态描述在结果列表里的两行截断覆盖。`site\index.html` 的样式缓存参数同步更新为 `20260710-card-wrap1`。

继续按用户反馈微调样式。地图悬停里的建筑 PNG 去掉了 `.building-chip` 的边框、背景和阴影，图标本身也去掉内层背景和阴影，只保留 PNG 与数量。全站滚动条改为当前深蓝黑轨道、金色到蓝色的滑块渐变，并统一使用圆角细滚动条。列表卡片改为按内容自然增高：国家和文化卡片取消固定最大高度，公司卡片取消固定最小高度、最大高度和一行裁切，主营建筑、名贵商品和标签行都允许按内容换行显示。

本轮续修按用户的新反馈调整了详情列表视图和地图悬停。地图悬停里的名称变体只保留在标题行，不再重复显示字段；地区和公司板块的资源改为建筑 PNG 图标，并分为“采矿”“其他资源”“农业”三行，不再缩写成“等几项”。详情展开后变为筛选、列表、详情三栏，隐藏地图和地图工具栏；在详情视图中点击列表卡片会更新右侧详情并留在详情列表视图，不再回到地图视图。国家引用统一显示为 `名称(TAG)`，地区列表摘要改为只显示 `开局归属：国家(TAG)`，不再重复显示战略区域；`市场接入度的价格影响` 在界面层缩写为 `MAPI`。全站搜索继续只服务搜索弹窗和详情跳转，不再参与具体板块过滤。国家、文化、地区、公司和意识形态板块移除了自动选中第一条结果的逻辑，初始进入板块时保持未选中状态。
