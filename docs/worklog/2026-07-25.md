# 2026-07-25：通用标签提示框布局

本次先提交设计记录 `fbcb4078`，随后以合并提交 `95987af1` 将 `codex/global-tag-tooltips` 截至 `c0f2b6bf` 的 15 个前置提交纳入 `main`。该基线为普通标签、引用标签、建筑图标和公司图标标签补齐了概念元数据、中文说明、文化关系内容和静态契约。

提交 `4c12ce79` 将原先只用于文化关系提示框的两列标题和分隔线改为通用结构。除意识形态外，概念标签都在左侧显示中文名与 ID，右侧显示类型；内容区显示说明、上下文或文化关系。链接标签显示“左键进入详情页”，带搜索数据的标签显示“右键进行筛选”，两种能力都不存在时不生成操作行。传承、语言等文化特质和特质组仍显示具体类型；意识形态保留法律态度、图标和说明组成的独立提示框。

静态验证依次运行了 `node --check site/app/components.js`、`node --check site/app/ui.js`、`node scripts/check_tag_tooltip_contracts.mjs`、`node scripts/check_right_panel_layout.mjs`、`node scripts/check_ui_ideology_contracts.mjs` 和 `node scripts/check_frontend_file_split.mjs`，均以零状态结束。按本次要求没有启动本地服务器或浏览器。
