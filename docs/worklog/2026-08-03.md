# 2026-08-03：多语言与地域功能合并发布

## 故障原因与修复

2026-08-02 的多语言版本已经发布，但当天稍后的地域地图版本从 `main` 分支构建并再次完整替换 `/var/www/vicdata/site`。该版本包含省份地形图和地区特质功能，却不包含多语言分支的 `app/i18n.js`、英语语言包和语言菜单，因此公开站点在第二次切换后失去了多语言内容。两个版本分别经过验证，问题发生在发布组合阶段。

本次在 `codex/multilingual-architecture` 分支合并 `main` 至 `61c46533`，合并提交为 `a43fbc93`。结果同时包含简体中文与英语、八个资料板块、省份地形图、地区特质筛选和地图提示。部署门禁提交 `ae994149`、`c9205a90` 要求主站和 Victorian Century 在切换前同时具备英语运行时、英语语言包、语言菜单、地形数据和地区特质控件。地图构建脚本在缺少 `state_regions.json` 时会以非零状态停止，避免用空地图覆盖现有文件。地域地图双语浏览器检查由 `65e3c5ce` 补充；移动端详情标题修复由 `d2e04205` 完成，390 像素下内部键移到标题下方，不再把英文名称压成逐字母换行。

## 验证与发布

发布前通过 19 项静态、数据和构建检查。历史版本夹具覆盖国家、文化、选中资源后的地域页、地区特质、法律、意识形态和公司路由。主站浏览器检查逐项扫描 2950 条英文详情，Victorian Century 扫描 3032 条，均覆盖国家、文化、地区、公司、意识形态、法律、科技和成就，以及 1440×1000 与 390×844 两种视口。英文页面未发现汉字、游戏格式标记残留或横向溢出。地形图和地区特质专项检查确认英语图例、提示数值、MAPI 统一标签、资源组合筛选和移动端地图正常。

服务器暂存目录 `/home/vicadmin/vicdata-stage-20260803-multilingual-region-combined` 由活动站点的硬链接副本建立，再以本地 `site/` 完整同步。2026-08-03 19:27:55 执行原子切换，活动目录仍为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260803-192755`。公开站点七个关键文件均返回 HTTP 200，SHA-256 与本地发布包一致；主站和 Victorian Century 随后的完整浏览器检查及地形图专项检查均通过。

## 后续发布约束

整站发布只能使用包含所有已上线功能的集成分支或发布构件。功能分支可以单独测试，不得直接完整替换活动站点。每项新的全站功能首次发布时，应同步增加部署门禁标识及其测试；当前门禁已经覆盖两站的多语言与地域地图功能。发布前必须对完整暂存目录执行文件、固定标识和关键哈希检查，切换后保留带时间戳的回退目录，并从正式域名运行桌面和 390 像素浏览器回归。这样可以在切换前拦截缺少旧功能的发布包，并在公网异常时保留明确的恢复位置。
