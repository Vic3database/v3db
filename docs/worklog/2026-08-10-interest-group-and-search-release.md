# 2026-08-10 利益集团与全局搜索更新发布

本次整理了新顶栏下的首页板块结构，补充利益集团板块及风味子页面，并修复国家地图点击后列表定位、全局搜索收录范围、结果行高、图标显示、公司图标居中和国旗宽度等问题。利益集团页面补充条件变体、国家风味、悬停提示和意识形态内容，并按确认名单整理国家风味名称。主站改动同步至 `site/vc/` 和根目录的 `Victorian Century Database/` 独立版。

首页公告新增“2026-08-10｜更新”，正文为“新增利益集团板块；优化板块结构；修复了全局搜索相关的一些问题。”公告来源为 `announcements.md`，发布数据为 `site/announcement-data.js`，缓存版本为 `20260810-interest-group-release1`。

发布前通过 34 项静态检查，范围包括公告、首页、全局搜索、国家地图联动、成就、法律、科技、经济资源、利益集团、多语言、发布包和 Victorian Century。浏览器回归通过主站中英文共享界面、成就板块、经济板块，以及主站、`site/vc/`、独立版三套国家地图列表定位。利益集团浏览器回归分别验证主站、`site/vc/` 和独立版，包含风味子页面、悬停、意识形态以及军队 5 国与 26 国互斥分组。`git diff --check` 通过。

`scripts/check_right_panel_layout.mjs` 与 `scripts/check_ui_ideology_contracts.mjs` 各有 8 项既有失败；从提交 `40c5029e` 导出的只读基线运行结果与当前工作区完全一致，本轮没有增加这些差异。外部代码审查工具安装时发生 TLS 握手失败，未作为发布依据。

提交范围包括本轮已跟踪的前端、构建与检查脚本，新增的国家地图定位、利益集团命名和本地化清理检查，以及 196 个被经济资源索引引用的生产方式图标。截图、缓存、人物审计临时脚本、`Victorian` 临时文件和未纳入当前实现的早期设计文档不进入提交。

主体更新提交为 `991149dd`，已推送至 `origin/main`。服务器候选目录 `/home/vicadmin/vicdata-stage-20260810-interest-groups-search` 从活动站点的硬链接副本建立，再与本地 `site/` 完整同步；候选目录含 11,532 个文件，九个关键文件的 SHA-256 均与本地一致。2026-08-10 18:09:59 完成首次原子切换，回退目录为 `/var/www/vicdata/site.previous-20260810-180959`。公开主站、公告、全局搜索索引、利益集团脚本、生产方式图片和 Victorian Century 入口等九个文件均返回 HTTP 200，哈希与本地一致。

发布后截图显示窄屏顶栏出现新结构与旧样式混用。原因是入口 `styles.css` 已更新缓存参数，其内部导入的 `foundation.css` 没有参数，`shell.css` 仍使用 8 月 6 日的参数，浏览器因而继续复用旧缓存。修复提交 `5af8aa83` 将三份样式统一为 `20260810-topbar-cache1`，并扩充双层导航检查：在 442×844 视口展开“内政”时，分类必须占满导航行，子菜单不得与其余分类标题相交。主站、`site/vc/` 和独立版本地回归均通过。服务器候选目录 `/home/vicadmin/vicdata-stage-20260810-topbar-cache` 的八个入口与样式文件哈希均与本地一致，2026-08-10 19:11:11 完成第二次原子切换，当前回退目录为 `/var/www/vicdata/site.previous-20260810-191111`；公开主站与 Victorian Century 的 442 像素浏览器回归均通过。
