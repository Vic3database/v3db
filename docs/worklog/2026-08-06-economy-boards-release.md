# 2026-08-06 建筑、商品与生产方式更新发布

本次将本地 `main` 的建筑板块、商品板块、Victorian Century 中英文经济内容及生产方式显示修正同步至 `https://vic3database.org/`。首页新增“2026-08-06｜更新”公告，正文分为“新增建筑板块和商品板块。”与“修复了一些bug。”两行。公告来源保存在 `announcements.md`，生成文件为 `site/announcement-data.js`。

发布前通过公告来源一致性、首页布局、主站共享多语言、主站建筑商品、Victorian Century 建筑商品及发布包检查。首页浏览器回归覆盖 1440×900 与 390×844 两种视口和简体中文、英语界面；主站与 Victorian Century 的建筑、商品路由均通过中英文浏览器检查。发布包共核对 1426 个文件。

服务器暂存目录 `/home/vicadmin/vicdata-stage-20260806-economy-boards` 从活动站点的硬链接副本建立，完整同步本地 `site/`。同步目录包含 11120 个文件；相对旧活动站点有 1566 个文件需要更新，逻辑文件量约 283 MB，`rsync` 实际发送约 602 KB、接收约 972 KB，其余数据由增量匹配复用。切换前核对公告、建筑与商品入口、生产方式商品流向、主站和 Victorian Century 的数据文件与图标，并确认 12 个关键文件的 SHA-256 与本地一致。

首次上传部署脚本时，Windows `scp` 保留了 CRLF，Debian `sh` 在 `set -eu` 处退出，活动目录和暂存目录均未移动。服务器将脚本转换为 LF 并通过 `sh -n` 后重新执行。原子切换于 2026-08-06 20:17:21 完成，活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260806-201721`。

切换后，公开站点 12 个关键文件均返回 HTTP 200，SHA-256 与本地一致。公开首页多语言回归、主站建筑商品回归和 Victorian Century 建筑商品回归通过；首页第一条公告的日期、标题和两行正文经浏览器结构化读取确认无误。服务器活动目录为 11120 个文件，回退目录存在，暂存路径已由原子切换移除。
