# 2026-07-31：资源地图自然色与资源上下文栏

## 界面与地图

地域资源筛选地图已取消战略区域文字水印。画布只保留地区储量数字，作物名称不再绘制在地图上。重置地图按钮右侧新增资源上下文栏，资源筛选有效时显示建筑图标、中文资源名和当前资料库版本；切回战略区域等其他地图模式时隐藏。建筑图标表没有对应项时，栏内改用资源主色圆点，因此 Victorian Century 独有资源不会产生损坏图片。

农业资源按作物分别使用低值和高值颜色。小麦、黑麦、稻米、玉米、杂谷、畜牧、葡萄、咖啡、茶叶、烟草、鸦片、香蕉、糖料、丝绸、棉花和染料各有独立渐变。煤、铁、铅、硫、金、渔业、捕鲸、林业、橡胶和石油保留原有终点色；金矿场继续继承金矿颜色。资源模式中可见陆地的不透明度由 210 提高至 232，低储量区域受底图干扰更小。国家、文化、特质、公司和战略区域地图没有改动。

## 验证

先将 `check_resource_map_colors.mjs` 改为新契约，旧实现因缺少小麦专属渐变而失败。实现后，该检查确认 1.13.9 数据中的 27 类资源均有颜色定义，16 种农业资源的两端色准确，农业水印相关函数和战略区域中心缓存已移除，资源模式不透明度和入口缓存版本已更新。

已执行 `node --check site/app/map.js`、`node --check site/app/runtime.js`、`node --check site/app/data.js`、`node scripts/check_resource_map_colors.mjs`、`node scripts/check_region_map_interaction.mjs`、`node scripts/check_country_map_selection.mjs` 与 `git diff --check`。主站和 Victorian Century 生成站均通过 Playwright 检查：小麦农场和铁矿显示正确的图标、名称与 1.13.9，画布没有“小麦农场”或“铁矿”文字。390 像素宽视口中，资源名称和版本保持同一行，上下文栏未越出视口。

执行 `node scripts/build_victorian_century_site.mjs --skip-vc-assets` 后，`site/app/map.js` 与 `Victorian Century Database/app/map.js` 的 SHA-256 均为 `0F50E10157F38600B2C93EA0D7E60BB13D95CD104626B80B6B3B2C3279BA5136`。浏览器截图 `screenshots/resource-map-wheat-context-20260731.png` 用于核对小麦资源图的工具栏位置和无水印效果；截图目录不纳入提交。
# 实心线性色阶

资源图随后改为完全不透明的线性色阶。资源值为 1 时使用各资源的浅色端，达到该资源的最大值时使用标准色端，数值在两端之间按线性比例插值；地图上的数字仍保留为精确储量。铁矿使用 `#cde0eb` 至 `#557b91`，多资源合并筛选使用 `#c9d6de` 至 `#58788a`。资源图海面固定为 `#c9dde2`，无资源陆地为 `#e9edeb`；纸色底图只保留给其他地图模式。

主站与 Victorian Century 的 `map.js` 和 `runtime.js` 已同步，最终 SHA-256 分别为 `7308CF51575382A6F281AD1FADC5DFE5022D5FC37370BBC2002E6A100A5FF17A` 和 `DC92623C1870AF4B856160EF181775F1971024EFA71D4B3A2355B61810ECCDA9`。两处页面均通过 Playwright 检查：小麦和铁矿资源层的非透明像素只有 255，铁矿图层中的旧黄色像素为 0，资源图标、名称、版本和无文字水印的检查也通过。铁矿截图保存在 `.codex/resource-map-solid-linear-iron.png`，不纳入提交。

资源图海域随后恢复纸质底图与原有纸色画布。资源陆地仍为 255 alpha，纸色只出现在海洋和未映射区域，因此不会让低储量地块重新泛黄。棉花色阶维持 `#deecf1` 至 `#8baebb`，本轮不另作调整。新的铁矿截图 `.codex/resource-map-paper-sea-iron.png` 已核对纸色海域、冷灰无资源陆地与蓝灰铁矿均同时存在，不纳入提交。

Victoria Century 已发布至 `https://vic3database.org/vc/`。发布包从已提交的 `9cc6972a` 与 `eef684da` 只读副本构建，避开工作区未提交的资源统计改动；远端只替换 VC 入口、样式和地图运行文件。两次原子切换后的回退目录分别为 `/var/www/vicdata/site.previous-20260731-150911` 与 `/var/www/vicdata/site.previous-20260731-151924`。公开页面检查确认铁矿筛选显示“铁矿 · 1.13.9”，`app/runtime.js` 和 `app/map.js` 均以 `20260731-resource-map-paper-sea1` 缓存参数返回 200。

资源上下文栏随后按站点配置区分资料库名称：原版继续显示数据版本 `1.13.9`，Victorian Century 显示 `Victorian Century/真实资源储量&耕地`，英文名称为 `Explorable Real-World Resources`。`map.js` 的缓存参数更新为 `20260731-vc-library-label1`，浏览器检查同时覆盖两种显示规则。

完整标签已发布至公开 Victorian Century 页面。发布前后均以浏览器选择小麦农场和铁矿确认工具栏文本，发布后的页面显示“小麦农场 · Victorian Century/真实资源储量&耕地”和“铁矿 · Victorian Century/真实资源储量&耕地”。远端只替换 `vc/index.html` 与 `vc/app/map.js`，原子切换后的回退目录为 `/var/www/vicdata/site.previous-20260731-154828`。

完整资料库名称在 1000×514 视口、筛选栏收起时曾越过资源上下文栏右边界。上下文栏现可在 340 像素内伸缩，窄于该宽度时文字在栏内省略；悬停版本文字可读取完整名称。浏览器检查覆盖资源已选中、筛选栏收起后的工具栏位置、容器滚动宽度和视口边界。

资料库名称随后改为直接完整显示，不再依赖省略号或悬停提示。宽屏保持单行；360×514 窄视口中，资源栏可自然换行，完整名称仍在工具栏与视口边界内。

# 楚科奇标签位置

原版资源地图中，`STATE_CHUKOTKA` 横跨世界图左右边界，旧的地域中心点按普通横坐标平均，煤矿等资源数字会被放到地图中部。`computeMapStateCenters()` 现保留普通地域的算术中心；当某个地域横向跨度超过半张地图时，把左边缘像素临时接到右侧再求平均，因此跨边界地域的标签仍停留在实际地域附近。

新增 `scripts/check_map_state_centers.mjs`。该脚本先用 12 像素宽的合成地图复现跨边界平均错误，再读取 `site/versions/1.13.9/map-data.js`，确认 `STATE_CHUKOTKA` 的中心点横坐标为 `8056.954942254989`，位于 8192 像素宽地图的右侧边缘。已同步更新 `site/index.html` 中的 `map.js` 缓存版本为 `20260731-map-center-wrap1`。

根站已发布到 `https://vic3database.org/`。远端阶段目录 `/home/vicadmin/vicdata-stage-20260731-191500-root-chukotka-center` 从当前线上站点完整复制，只替换 `index.html` 与 `app/map.js` 后执行原子切换；回退目录为 `/var/www/vicdata/site.previous-20260731-183111`。公开页面检查确认入口加载 `app/map.js?v=20260731-map-center-wrap1`，远端 `index.html` 与 `app/map.js` 的 SHA-256 分别为 `b32d72bbde72ee37ec96b14c1bd79d292df93641e5b84618d4a07485742327f9` 与 `6878b7d15a1dc7e62d426f39fcd08334b2a168af14479eb627542786f8d8a36e`。Playwright 在公开站点选择煤矿后，楚科奇的 `36` 标签绘制于 `x=8056.954942254989, y=305.0477450488641`。
