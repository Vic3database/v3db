# 工作记录：2026-07-17

## 2026-07-17 科技树上线与服务器发布流程

最终外网核验结果：`https://vic3database.org/`、`https://www.vic3database.org/`、`/index.html`、版本数据索引、地图数据、图标和前端路由回退均返回 HTTP 200；两个 HTTP 域名均 301 到对应 HTTPS 地址；地图数据在 `Accept-Encoding: gzip` 下返回 `Content-Encoding: gzip`；首页与路由回退返回 `Cache-Control: no-cache`，数据与静态资源返回 30 天不可变缓存；上述安全响应头均存在。Nginx 为 active，证书续期定时器为 enabled、active，Vicdata 访问日志已写入，错误日志为空。`free -h` 显示总内存约 1.9 GiB，进程实际使用 282 MiB，可用内存约 1.6 GiB，缓存约 1.7 GiB；交换分区仅使用 512 KiB，当前 2 核 2 GiB 规格足以托管静态站。

最终启用的 Nginx 配置位于 `/etc/nginx/sites-enabled/vicdata`，其校验值为 `e66654f7eb999213ce585a7c5ae10ce7aba508d360d19578883bd73a9439be3d`。本机同步保存了候选配置 `deploy/nginx/vicdata.conf`，后续调整后应上传到 `/home/vicadmin/vicdata.conf`，再由 VNC root 会话复制到实际启用路径并执行 `nginx -t && systemctl reload nginx`。配置内容包括：80 端口跳转 HTTPS，gzip 压缩，首页与路由回退 `no-cache`，JavaScript、样式、图片、图标等静态资源 `public, max-age=2592000, immutable`，以及 HSTS、`X-Content-Type-Options: nosniff`、`Referrer-Policy: strict-origin-when-cross-origin`、`X-Frame-Options: SAMEORIGIN`。独立日志为 `/var/log/nginx/vicdata.access.log` 与 `/var/log/nginx/vicdata.error.log`。

已通过 Certbot 为 `vic3database.org` 与 `www.vic3database.org` 签发 Let’s Encrypt 证书。初次签发因 Nginx 的 `server_name _;` 无法自动安装，改为 `server_name vic3database.org www.vic3database.org;` 后，以 `certbot --nginx --redirect -d vic3database.org -d www.vic3database.org` 选择重装现有证书完成部署。证书主题为 `CN=vic3database.org`，有效至 2026-10-12 12:48:03 UTC；`certbot.timer` 处于 enabled、active 状态。IP 地址不能匹配该域名证书，维护时使用 IP 访问 HTTPS 会显示证书名称不匹配，正式访问应使用域名。

根路径首次返回 403，原因是 Nginx 写成了 `index nidex.html;`；在 VNC root 终端修正为 `index index.html;` 后，`curl -I http://127.0.0.1/` 返回 HTTP 200。域名 `vic3database.org` 由 Namecheap 解析，根域名 A 记录指向 `103.214.174.247`，`www` 使用 CNAME 指向根域名；不设置 AAAA 记录。公共 DNS 传播期间 Cloudflare、Google、腾讯已解析到服务器，阿里曾短暂保留 GitHub Pages 缓存，未阻碍后续签证书。

本机将 `site` 打包为发布归档并排除了 `debug-*` 文件和 `*.log`。首次尝试通过 PowerShell 管道直接传输 tar 数据时，二进制流被处理而导致远端解压失败；站点目录未切换。随后改为先生成 `C:\Users\SamuY\AppData\Local\Temp\vicdata-release-20260714.tar`，再用 SCP 上传、在远端解压到暂存目录，并在确认存在 `index.html` 后切换目录。当前 `/var/www/vicdata/site` 共 4,448 个文件，约 971 MiB；旧目录保留为 `/var/www/vicdata/site.previous`，便于必要时回退。发布目录中不含调试截图与本地服务日志。

## 2026-07-17 开局附属国宗主颜色

发布后公网检查显示 `https://vic3database.org/`、`https://vic3database.org/versions/1.13.9/data-technologies.js` 与 `https://vic3database.org/assets/technologies/nitrogen_fixation.webp` 均返回 HTTP 200。后续发布沿用“本机打包、SCP 上传、服务器临时目录校验、VNC root 输入单条脚本命令、再从公网检查资源”的流程。GitHub 仍用于保存源码、提交记录与协作，不作为线上静态站部署通道。

由于 `vicadmin` 的 SSH 会话需要 sudo 密码，目录切换由 VNC 的 root 终端执行。为避免 VNC 粘贴和大小写问题，本机已将部署脚本写入 `/home/vicadmin/deploy-vicdata.sh`，权限为 `700`。root 终端只需输入 `sh /home/vicadmin/deploy-vicdata.sh`；脚本会校验临时目录，删除旧的 `site.previous`，将当前 `site` 移为 `site.previous`，把临时目录切换为新站点，修正所有者并执行 `nginx -t` 和 `systemctl reload nginx`。截图确认脚本输出 `vicdata deploy complete`，Nginx 语法检查成功。

线上部署不再依赖 GitHub Pages，实际发布目标为香港服务器 `103.214.174.247` 的 `/var/www/vicdata/site`。本轮将当前 `site` 打包为 `C:\Users\SamuY\AppData\Local\Temp\vicdata-release-20260717-155738.tar` 并上传，在服务器 `/home/vicadmin/vicdata-stage-20260717-155738` 解压校验。临时目录共 4,522 个文件、约 157 MiB，已确认包含 `index.html`、`versions/1.13.9/data-technologies.js` 与 `assets/technologies/nitrogen_fixation.webp`。

科技图标已由 179 张 PNG 转换为 WebP，页面改为引用 `.webp` 文件。图标总大小从 12,229,642 字节降至 2,784,698 字节。`node --check site/app.js`、`node scripts/check_technology_board_contract.mjs`、`node scripts/check_technology_database.mjs` 与 `node scripts/check_data_chunking.mjs` 均通过；数据量为生产 57 项、军事 58 项、社会 64 项，共 179 项科技。

科技板块已完成生产、军事、社会三套独立科技树页面，使用固定网格定位、科技前置与后续关系线、右侧详情页、板块内搜索和全局搜索入口。顶栏新增灯泡图标的“科技”入口；科技树工具栏包含类别下拉框、搜索框与旋转箭头的重置视图按钮。科技详情未选中时不显示右栏，选中后显示前置科技、后续科技、修正效果、关联法律和关联公司。
