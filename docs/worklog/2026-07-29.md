# 2026-07-29：Victorian Century 数据更新、素材整理与独立站前端同步

本轮以 Steam Workshop 条目 `3219394272` 为数据来源更新 Victorian Century。已安装版本与远端清单均为 `7782240185317114361`，时间为 2026-07-27 10:52:01；资料库生成时间为 2026-07-28 15:15:50。更新完成后，`node scripts/check_victorian_century_update.mjs --check-only --json` 返回 `up_to_date`。当前数据包含 732 个国家、320 个文化、781 个地区、231 家公司、228 种意识形态、148 条法律和 180 项科技；地图源数据为 8192×3616。

## 素材整理

新增 `scripts/sync_victorian_century_assets.py`，按当前 Victorian Century 数据实际引用的图像补入独立站，不复制模组中未被资料库使用的日志、事件、决议和音乐资源。本轮补入 42 项素材：10 个公司图标、6 个法律图标、2 个意识形态图标与 24 个声望商品图标。其中公司、法律和意识形态共生成 18 份 WebP；声望商品与省份地图继续只使用 PNG。脚本会生成 `Victorian Century Database/assets/victorian-century-assets.json`，同时以像素比对保护 Victorian Century 自有素材，避免主站同名文件覆盖。

图像展示沿用原版站的规则：公司、法律和意识形态优先读取 WebP，加载失败时回退 PNG；声望商品保持 PNG。`site/app/components.js` 同时修正了公司图标路径处理，带有 `.png` 或 `.dds` 后缀的 Victorian Century 公司图标都能指向正确文件。

## 独立站前端与构建

新增 `scripts/build_victorian_century_site.mjs`，默认将主站 `site/` 的模块化应用、样式和共用资源同步至 `Victorian Century Database/`，保留 Victorian Century 自己的数据、地图和新增素材。构建过程写入独立的 `index.html`、`victorian-century-config.js` 与站点清单，移除版本选择、版本分组、更新日志、公告和新闻入口。前端通过 `window.VICTORIAN_CENTURY_SITE_CONFIG` 读取本地数据分块与地图；主站继续使用原有的版本化读取逻辑。

`scripts/build_wiki.mjs` 新增 `--legacy-data`，用于在分块数据外保留兼容用的 `data.js`。`scripts/check_victorian_century_update.mjs` 已串接该参数及独立站构建，因此模组更新后可由同一条更新脚本完成数据提取、地图生成、兼容文件写入、素材同步和前端重建。新前端本身不加载 `data.js`，该文件供旧页面和回退场景使用。

## 验证与维护

新增 `scripts/check_victorian_century_standalone_site.mjs`，检查独立页标题、配置、七类数据分块、地图尺寸、11 个模块、路由限制和 WebP 回退逻辑。新增 `scripts/check_victorian_century_browser.mjs`，在浏览器中验证首页以及国家、文化、地区、公司、意识形态、法律、科技页面；地区地图重置和 Benz 公司图标的 WebP/PNG 回退均已通过。验证时使用 `http://127.0.0.1:8877/index.html`，画布渲染尺寸为 4096×1808，页面没有控制台错误。`git diff --check` 也已通过。

常规维护先运行 `node scripts/check_victorian_century_update.mjs --check-only --json` 判断是否需要更新；需要单独重建独立站时运行 `node scripts/build_victorian_century_site.mjs`；素材状态可用 `python scripts/sync_victorian_century_assets.py --check` 检查。生成目录 `Victorian Century Database/` 由 `.gitignore` 忽略，需提交或发布时应提交本轮新增和修改的 `scripts/`、`site/app/` 与设计记录，不把生成目录作为源码提交。

## 主站入口与正式发布

主站首页在说明区与资料分类之间新增 Victorian Century 资料库入口，链接为相对路径 `vc/`；右上角选择器改为“Victoria 3 原版 1.13.9”与“Victorian Century”两个资料库选项。独立站构建脚本新增 `--publish-target`，将生成结果同步到受 `.gitignore` 忽略的 `site/vc/`；独立站 HTML 会移除主站入口，且不显示资料库选择器。

发布脚本在服务器切换前校验 `vc/index.html`、数据索引、地图数据、配置文件与省份地图。2026-07-29 将 10,849 个文件同步到 `/home/vicadmin/vicdata-stage-20260729-115635-vc`，其中 6,805 个文件传输约 1.03 GB；切换后活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-115918`。`https://vic3database.org/vc/` 与 `https://vic3database.org/vc/data-index.js` 均返回 HTTP 200。公开浏览器检查确认主站卡片和选择器均能进入 VC，VC 首页及国家、文化、地区、公司、意识形态、法律、科技七个板块均已渲染且没有控制台错误。

同日修正资料库选择器的两个问题。Chrome 在 Windows 上的原生选项列表保持系统白色底色，却会读取网页指定的文字颜色；此前使用浅色文字造成未选中项不可读，现将顶部选择器选项明确设为 `#181715` 深色文字。VC 独立站构建时原本移除了选择器，现改为保留 Victorian Century 与“Victoria 3 原版 1.13.9”两个选项；选择原版会以相对路径 `../` 返回主站。回程选择器修正发布到 `/home/vicadmin/vicdata-stage-20260729-121434-vc-fix`，活动目录切换后保留回退目录 `/var/www/vicdata/site.previous-20260729-121603`。公开浏览器回归确认主站与 VC 可双向切换。

随后依据实际截图修正了原生菜单颜色。2026-07-29 以 `/home/vicadmin/vicdata-stage-20260729-122425-option-color` 发布新的样式缓存版本 `20260729-vc-entry3`，旧版保留在 `/var/www/vicdata/site.previous-20260729-122543`。公开页面实际返回未选中选项文字 `rgb(24, 23, 21)`，背景为系统原生白色菜单。

## Victorian Century 新增或调整标签与筛选

`scripts/build_wiki.mjs` 新增 `--baseline-database`。Victorian Century 构建以 `database/vic3_1.13.9` 为基线，按稳定化后的内容比较记录：忽略条目编号、来源路径与先前标签字段，保留数组顺序和其他数据字段。新增记录写入 `vc_change_kind: "added"`，同键但内容不同的记录写入 `"adjusted"`。自动更新脚本也传入该基线，因此后续 Workshop 更新会重新计算标签。

本次对比得到国家 2 项新增、531 项调整；文化 3 项新增、190 项调整；文化特质 3 项新增；地区 675 项调整；地区特质 240 项新增、104 项调整；公司 10 项新增、97 项调整；意识形态 56 项新增、172 项调整；法律 10 项新增、53 项调整；科技 1 项新增。利益集团特质同时标记 175 项新增和 39 项调整。文化特质与地区特质的嵌入引用会继承该字段，列表和详情中的实际特质也会显示“VC新增/调整”。

资料页通过统一的“资料标签 / VC新增/调整”条件筛选国家、文化、地域、公司、意识形态与法律；科技树的侧栏原本隐藏，故将相同条件放入科技树顶部控制栏。原版数据没有这一字段时，该侧栏条件保持隐藏。`scripts/check_victorian_century_change_tags.mjs` 会以新建临时目录重建两套数据并逐类核对标签，`scripts/check_victorian_century_browser.mjs` 会在本地 Chrome 中操作七类页面的筛选条件。发布前的本地验证均已通过。

2026-07-29 以 `/home/vicadmin/vicdata-stage-20260729-vc-change-tags` 进行增量发布。预演显示 10,849 个文件中有 37 个需同步，传输数据为 19,343,361 字节，其余由当前活动版本复用。切换后活动目录仍为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-130848`。`https://vic3database.org/vc/`、`data-index.js` 和 `data-technologies.js` 均返回 HTTP 200；页面包含缓存版本 `vc-change-tags1` 与标签筛选控件，科技数据包含 `vc_change_kind` 字段。

## 公司列表图标定位

VC 自有的公司图标通过 `picture` 元素提供 WebP 与 PNG 回退，普通公司图标仍直接使用 `img`。公司列表的网格定位规则原先只命中内部 `img`，因此 WebP 图标的包装层被浏览器自动放进首行，图标相对标题顶部上移约 13 像素。现为公司标题内的 `picture` 包装层补上与图标相同的网格列、跨行和尺寸规则，并以 `line-height: 0` 去除行内基线高度。浏览器回归检查同时读取 `company_admiralty_rijkswerf` 与 `company_a_markwald_and_company`：修复前两者相对顶部为 `-13.953125px` 与 `-1px`，修复后均为 `-1px`。该检查保留在 `scripts/check_victorian_century_browser.mjs`。

本次以 `/home/vicadmin/vicdata-stage-20260729-vc-company-icon-grid` 做增量同步，10,849 个文件中有 8 个更新，传输文件总量为 92,544 字节。切换后活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-132729`。公开站公司页验证 `company_admiralty_rijkswerf` 的 WebP 图标与普通 PNG 公司图标均相对标题顶部为 `-1px`，加载的样式入口为 `styles.css?v=20260729-vc-company-icon-grid1`。

## VC 新增与调整筛选拆分

原有的“VC新增/调整”筛选改为两个可同时选择的条件：“VC新增”与“VC调整”。未选择时不过滤；只选择其中一项时仅保留对应的 `vc_change_kind`；两项同时选择时保留两类条目，结果与原合并筛选一致。列表和详情中的徽章也改为同名的单一标签。国家页的新增筛选返回帝国联邦与罗马帝国 2 项，调整筛选返回 531 项；地区没有新增地区，新增筛选返回 0 项，调整筛选返回 675 项。

科技树顶部同样使用两个筛选按钮。检查过程中发现新增科技“垂直整合种植园”没有预设网格坐标，旧布局会直接略过该条目。科技树现会在对应时代的首个未占用网格位置放置缺少预设坐标的科技，保留已有科技坐标不变。`scripts/check_victorian_century_browser.mjs` 依次验证国家、文化、地区、公司、意识形态、法律和科技的仅新增、两项同时选择、仅调整三种状态；科技新增筛选目前可见图形节点与移动列表各一项。

本次以 `/home/vicadmin/vicdata-stage-20260729-vc-change-kind-split` 做增量同步，10,849 个文件中有 16 个更新，传输文件总量为 750,644 字节。切换后活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-135553`。公开站使用 `vc-change-kind-split1` 脚本缓存版本验证：国家“VC新增”筛选只返回 `IMP` 与 `RME`，科技“VC新增”筛选只返回 `united_fruit_banana_tech`。

科技的 21 项旧“VC调整”标记只来自 `references` 字段，即公司或法律对科技的引用关系变化；科技的类别、时代、前置、效果与说明没有变化。比较规则现只对科技集合忽略该字段，仍比较其他科技字段，故仅保留 `united_fruit_banana_tech`（垂直整合种植园）的“VC新增”标记。浏览器回归同时要求科技“VC调整”筛选返回 0 项。

本次以 `/home/vicadmin/vicdata-stage-20260729-vc-tech-reference-tags` 做增量同步，10,849 个文件中有 16 个更新，文件总量为 117,186,850 字节；rsync 实际发送 128,944 字节，其余内容与活动站点复用。切换后活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-142609`。公开科技页验证“VC调整”为 0 项，“VC新增”只返回 `united_fruit_banana_tech`。

“垂直整合种植园”在生产科技树固定为第 8 列、第 1 行，养蚕学位于第 10 列、第 1 行，故该节点位于养蚕学左侧两格。科技板块移除了“VC新增”和“VC调整”按钮，科技树不再继承其他页面的 VC 差异筛选状态，节点本身的“VC新增”标签保留。本次以 `/home/vicadmin/vicdata-stage-20260729-vc-tech-layout` 做增量同步，10,849 个文件中有 6 个更新，文件总量为 177,388 字节，rsync 实际发送 9,615 字节；切换后回退目录为 `/var/www/vicdata/site.previous-20260729-143529`。公开页面加载 `boards.js?v=20260729-vc-tech-layout2`，未找到两个筛选按钮，实测横向间距为 `-166px`。

## 新增国家旗帜

帝国联邦 `IMP` 与罗马帝国 `RME` 原先没有网站旗帜映射。新增 `scripts/build_victorian_century_country_flags.mjs`，将原版的旗帜纹章定义与 Victorian Century 的覆盖定义合并后渲染为网站 PNG。旗帜放在独立目录 `site/assets/victorian-century-flags/`，常规 `site/assets/flags/` 重建不会删除这些素材；补充映射由 `site/assets/victorian-century-flags.js` 在通用国旗数据加载后合并。

帝国联邦提供标准联邦旗、英国旗和含印度图案的 3 个变体，默认使用标准联邦旗。罗马帝国提供君主制、共和制、委员会和神权制 4 个变体，资料库无法从静态数据判定实时政体，故默认显示君主制旗帜。静态检查确认国旗数据由 736 个国家、1,640 个变体增至 738 个国家、1,647 个变体；本地 Chrome 确认 7 张旗帜均以 240×144 正常显示。

本次以 `/home/vicadmin/vicdata-stage-20260729-vc-country-flags` 做增量同步。预演显示新增 22 个文件，实际传输 20 个文件、219,044 字节；切换后活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-145504`。公开站点的定向浏览器检查确认 `IMP` 详情页加载 3 个变体，`RME` 详情页加载 4 个变体，默认图像路径分别为 `assets/victorian-century-flags/IMP/IMP.png` 与 `assets/victorian-century-flags/RME/RME_Flag_Monarchy.png`。

## 地区特质差异标签

地区列表此前同时显示地区本身的“VC调整”以及每项地区特质的“VC新增/调整”，同一行会出现重复标签。列表现在仍保留地区特质名称，但隐藏特质自身的差异标签，只显示地区本身的一项“VC调整”。地区详情的“地区特质”字段和“地区特质效果”卡片继续显示特质差异标签；效果卡片将标签移入特质中文名称的 `strong` 元素，紧贴中文名显示，不再放在卡片右侧。

新增 `scripts/check_victorian_century_region_trait_badges_browser.mjs`，以斯韦阿兰为回归样本：列表必须为 0 个“VC新增”和 1 个“VC调整”；详情必须保留滑铁卢运河、季节性冰封的“VC新增”和斯堪的纳维亚森林的“VC调整”，且三个标签均位于中文名元素内部。本地完整 VC 浏览器回归、差异标签数据校验和独立站校验均通过。

本次以 `/home/vicadmin/vicdata-stage-20260729-vc-region-trait-badges` 做增量同步。预演与实际均为 6 个文件更新，文件总量为 260,374 字节，rsync 实际发送 5,510 字节；切换后活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-150736`。公开站点浏览器检查确认斯韦阿兰列表为 0 个“VC新增”、1 个“VC调整”，详情中的 3 个特质标签均在中文名后。

截图显示地区特质的差异标签与中文名贴得过紧，现为 `.trait-card-content .rule-head strong > .tag-vc` 增加 `margin-left: 6px`，仅影响详情中的特质效果卡片。浏览器回归将实际间距限制在 5 至 7 像素；本地与公开站点均读取到滑铁卢运河、季节性冰封和斯堪的纳维亚森林的间距为 6 像素。样式入口更新为 `styles.css?v=20260729-vc-region-trait-badges2`。

本次以 `/home/vicadmin/vicdata-stage-20260729-vc-region-trait-badge-gap` 做增量同步。预演与实际均为 6 个文件更新，文件总量为 105,816 字节，rsync 实际发送 2,940 字节；切换后活动目录为 `/var/www/vicdata/site`，回退目录为 `/var/www/vicdata/site.previous-20260729-153727`。

随后根据详情页截图将地区特质中文名与差异标签的间距由 6 像素增至 12 像素。`scripts/check_victorian_century_region_trait_badges_browser.mjs` 先以 12 像素断言验证旧站失败，三个样本均实测为 6 像素；更新后，本地 `site/vc/` 中滑铁卢运河、季节性冰封和斯堪的纳维亚森林均为 12 像素。样式资源版本更新为 `20260729-vc-region-trait-badges3`。本次只重建了本地独立站目录与 `site/vc/`，未上传服务器。

本地以 `file://` 打开时，浏览器不会将目录路径自动解析为首页；`vc/` 和 `../` 会分别显示 VC 目录与主站目录的文件索引。主站首页入口和资料库选择器现明确指向 `vc/index.html`，VC 独立站的资料库选择器明确指向 `../index.html`。主站入口与选择器的缓存版本一并更新。`scripts/check_victorian_century_main_entry_browser.mjs` 在本地 Edge 的 `file:///.../site/index.html` 路径下检查主站进入 VC、VC 返回主站；完整 VC 浏览器检查也将返回目标固定为 `../index.html`。本次未上传服务器。
